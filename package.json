{
  "name": "opengraph-creator-monorepo",
  "version": "0.1.8",
  "private": true,
  "type": "module",
  "description": "Agent-first local Open Graph Creator studio with editable layered projects.",
  "license": "MIT",
  "scripts": {
    "clean": "node scripts/clean.mjs",
    "build": "npm run clean && npm run build -w @opengraph-creator/core && npm run build -w @opengraph-creator/render && npm run build -w @opengraph-creator/studio && npm run build -w opengraph-creator",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint .",
    "smoke:workflow": "node scripts/workflow-smoke.mjs",
    "smoke:studio": "node scripts/studio-smoke.mjs",
    "smoke:package": "node scripts/package-install-smoke.mjs",
    "smoke:agent-handoff": "node scripts/agent-handoff-smoke.mjs",
    "smoke:agent-next-action": "node scripts/agent-next-action-smoke.mjs",
    "dev:studio": "npm run dev -w @opengraph-creator/studio",
    "opengraph-creator": "tsx packages/cli/src/index.ts"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@types/node": "^22.10.2",
    "@types/react": "^18.3.18",
    "@types/react-dom": "^18.3.5",
    "@typescript-eslint/eslint-plugin": "^8.18.1",
    "@typescript-eslint/parser": "^8.18.1",
    "eslint": "^9.17.0",
    "playwright": "^1.60.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^4.0.15"
  }
}
